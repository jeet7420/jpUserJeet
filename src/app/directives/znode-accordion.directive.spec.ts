import { ZnodeAccordionDirective } from './znode-accordion.directive';

describe('ZnodeAccordionDirective', () => {
  it('should create an instance', () => {
    const directive = new ZnodeAccordionDirective();
    expect(directive).toBeTruthy();
  });
});
